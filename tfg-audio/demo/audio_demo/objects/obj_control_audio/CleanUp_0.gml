external_call(global.ext.shutdown);

external_free(global.ext.init);
external_free(global.ext.shutdown);
external_free(global.ext.setTempo);
external_free(global.ext.tplay);
external_free(global.ext.tstop);
external_free(global.ext.tick);
external_free(global.ext.getBeat);

external_free(global.ext.songLoad);
external_free(global.ext.songPlay);
external_free(global.ext.songStop);

external_free(global.ext.playQ);